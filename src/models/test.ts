export default interface Test {
  testId: string;
  hospitalId: string;
  name: string;
  description?: string;
  price: number;
  roomNumber?: string;
  floorNumber?: string;
  createdAt: string;
  updatedAt: string;
}
